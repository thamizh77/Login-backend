# Login Backend – Node.js

This is the backend for a login page built using Node.js and Express.

## Description
This backend provides a login API that checks email and password using mock data.
It returns success for valid credentials and error message for invalid credentials.

## Technologies Used
- Node.js
- Express.js
- CORS

## How to Run
npm install
node index.js

The server runs on port 5001.
